import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
import { globalStats } from './globalStats.component';

@NgModule({
  declarations: [globalStats],
  imports: [CommonModule],
  exports: [globalStats]
})
export class globStatisticsModule {}
